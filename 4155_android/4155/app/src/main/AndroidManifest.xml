<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a4155">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.login.mammaLeones"></activity>
        <activity android:name=".ui.login.Salsaritas" />
        <activity android:name=".ui.login.Dominos" />
        <activity android:name=".ui.login.FusionCafe" />
        <activity android:name=".ui.login.PandaExpress" />
        <activity android:name=".ui.login.crownCommons" />
        <activity android:name=".ui.login.DelivSettings" />
        <activity android:name=".ui.login.customerSettings" />
        <activity android:name=".ui.login.Profile" />
        <activity android:name=".ui.login.Home" />
        <activity android:name=".ui.login.directions" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ui.login.Directions"
            android:label="@string/title_activity_directions" />
        <activity android:name=".ui.login.Subway" />
        <activity android:name=".ui.login.Bagel" />
        <activity android:name=".ui.login.Wendys" />
        <activity android:name=".ui.login.Starbucks" />
        <activity android:name=".ui.login.Chick" />
        <activity android:name=".ui.login.Bojangles" />
        <activity android:name=".restauraunts" />
        <activity
            android:name=".restauraunt"
            android:label="@string/title_activity_restauraunt"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>